import { defineConfig } from "@decky/rollup";

export default defineConfig({
  // Decky's rollup preset wires up React/JSX, the @decky externals and the
  // frontend bundle output at dist/index.js.
});
